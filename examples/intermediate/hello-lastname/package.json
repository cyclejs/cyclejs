{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "author": "Andre Staltz",
  "license": "MIT",
  "dependencies": {
    "@cycle/dom": "14.2.x",
    "@cycle/isolate": "1.4.x",
    "@cycle/xstream-run": "4.1.x",
    "watchify": "^3.8.0",
    "xstream": "9.2.x"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.x",
    "babel-register": "^6.4.x",
    "babelify": "^7.2.0",
    "browserify": "13.0.x",
    "mkdirp": "0.5.x",
    "typescript": "2.0.x",
    "typings": "^1.0.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebrowserify": "mkdirp dist && typings install && tsc",
    "browserify": "browserify lib/index.js -t babelify --outfile dist/main.js",
    "start": "npm install && npm run browserify && echo 'OPEN index.html IN YOUR BROWSER'"
  }
}
